# D：篇内查找

起点 `9bee872`。Reader 增加当前分段查找、匹配行底色、上一处/下一处循环定位。
查找使用已有 localization 生成的当前 primary/secondary 文本及可见说话人，
不搜索隐藏角色身份，不请求其他分段，不加载媒体。NFKC、忽略空白及大小写
用于匹配，正文原文和译文均不改写。不使用正则查询或 HTML 注入。

选中结果发出 locate，App 验证当前 ready 文档确实包含该 row 后更新原有
readingRowId 和 URL（replace）；没有新建另一套阅读定位状态。刷新恢复选中句。
查询本身仅保存在组件内，切换分段清空，刷新/离开后不保留；不是阅读历史存储。
无结果时两个定位按钮禁用。非 ready 状态不显示查找栏。

验证：verify:reading 与 source-only 构建通过。Browser 390×844 在第二章序章
第二段查询 315 得到两处，依次定位到 step-8 与 step-37，URL 同步；无结果提示
和按钮禁用正常，刷新后仍选中 step-37，临时查询清空。无横向溢出、canvas 为 0，
验收日志无 error/warn。手机截图检查后修正查找标签换行。构建保留现有大分块提示。

这只是篇内搜索；跨分段全文搜索、本地阅读记录和更多入口仍待后续推进。
长稳继续后移，E2 未接管。
