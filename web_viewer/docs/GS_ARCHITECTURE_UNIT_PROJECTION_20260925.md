# GS 组合投影瘦身：2026-09-25

- 输入分支 `codex/gs-architecture-rebuild`，开始于 `008b771`。原 r4 组合详情重复携带成员完整卡片数组，最大单页为 755,491 字节；页面只消费卡片数量、稀有度数量、小剧情数量及单卡面数量。
- 生成器为组合目录投影封面、成员与数量统计；组合详情保留实际页面需要的组合、成员、统计、活动、歌曲与剧情，不再内嵌卡片数组。当前 checkout 的生产选择器产出 16 个组合，新详情视图最大约 30 KB，目录概要最大约 3.4 KB。未修改原始语料或既有页面。
- `node --test readmodels/tests/*.test.mjs` 的 27 项通过；新增回归用大体积卡片记录确认组合产物只含统计。此时仅调整生成器；新的固定 release 候选与页面接线在后续批次核验。
