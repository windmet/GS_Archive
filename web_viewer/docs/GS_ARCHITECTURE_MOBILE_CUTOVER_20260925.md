# Mobile communication read-model cutover (2026-09-25)

Starting HEAD: `62ba319`. The attached architecture package is reference material; source behavior and data come from this checkout. The producer was committed as `ce0ff96` before candidate generation.

The producer uses the checkout's communication selectors to project 49 idol and 16 unit records. Each idol leaf contains only that idol's personal, phone and random-topic bundles, scoped card and personal-story unlock references, room text and source evidence. Each unit leaf contains only that unit's conversations and unlock references. The page no longer requests three full communication indexes for Mobile entry, tab changes, idol/unit switching or Player return. Unmigrated pages still retain their legacy loaders.

Local candidate `E:\GS_readmodels_candidate_20260925_r17`, release `bc90adb8d88614a9d7339600ead42b6a2667ebba2f00a3448bd03014f1b177af`: `verify_artifacts` verified 3,036 files; the inline bootstrap is 12,286 bytes. The producer's 29 tests passed, including real-source parity across every idol and unit for bundle identity, title, ordering, counts and random-topic step boundaries. All card/story unlock targets in these leaves resolved against their source records. `npm run verify:communication-presentation`, `npm run verify:archive-navigation-state`, `npm run verify:archive-async-navigation` and `npm run build:check` passed. The build output is the reusable `.analysis/build-check` code build without a public corpus copy.

In the local in-app Browser at `127.0.0.1:5177`, the direct Mobile route displayed 天ヶ瀬 冬馬 with 20 personal items, 9 phone items, 6 unit items and 5 random topics. The four tabs displayed their expected records. A random topic opened Player with `start_step=2&end_step=6` and returned to the random tab. A card unlock opened its card detail and returned; a phone unlock opened personal story section `20102`, episode `2010208`, and returned. The next idol button loaded 御手洗 翔太; the unit button moved from Jupiter to DRAMATIC STARS, and refreshing the unit deep link retained that selection. Portal → 互动 → idol picker → Mobile also rendered the selected idol. Desktop screenshots were inspected for personal and unit views. This Browser path did not expose request or console logs, so the absence of legacy fetches is established by source-path review and route behavior rather than a captured network trace.

This is local code, projection and Browser acceptance. It is not a full public-asset package, deployment, physical-device, mobile viewport, or long real-media acceptance. The full route cutover contract remains unpromoted.
