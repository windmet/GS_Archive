# Story collection read-model cutover, 2026-09-25

The attached architecture guide remained a reference; the active checkout determined route behavior. This batch moves `story_collection` to an 80-entry read-model directory and one selected detail leaf. The directory covers 3 main, 16 unit, 10 extra and 51 birthday collections, including 10 entries with legacy section aliases. The verified local r12 candidate is `E:\GS_readmodels_candidate_20260925_r12`, release `fa2f82602e1c8e0fedc2fcc64c7626c9f02c77d9134fa12120448f7649e84dbc`. Its 2,961 artifacts verify; the inline bootstrap is 11,509 bytes.

Direct collection routes validate domain and section identity before rendering. Story catalog, song relation, community resource and idol birthday links open the selected leaf; stale responses cannot replace a newer navigation. A failed selection can be retried. Reader and Player retain return context, including a refreshed Player URL. Returning from a direct collection route prepares the legacy story catalog. The story catalog itself and the mobile archive still require their own cutovers.

Verification: `npm run build:check`, startup, async navigation, route, presentation and song relation regressions passed; the new collection async navigation test covers latest selection, supersession, alias context and retry. In-app Browser on the local 5176 QA server confirmed main and unit chapter direct loads, an extra legacy alias, idol birthday entry/return, Reader return, refreshed Player return, invalid collection fallback and a populated catalog on Back. Browser asset inventory for the direct main route listed the collection index, two directory pages, one selected leaf and the separate Reading manifest, with no `/data` startup batch. The page rendered without framework overlay or relevant console warnings/errors at the checked state; default mobile and temporary 1280×800 desktop screenshots showed readable content. This is local Browser evidence, not real-device or full-media acceptance. No full asset package or deployment was created.
