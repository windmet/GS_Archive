# GS 卡池档案 read-model 接线：2026-09-25

## 基线与产物

- 输入分支 `codex/gs-architecture-rebuild`，开始于 `d13b982`。投影补充单页搜索所需的卡片摘要、复刻标志及旧索引统计，先以 `2986faa` 单独提交并推送。由已提交的投影生成只读候选 `E:\GS_readmodels_candidate_20260925_r6`，release `6a22124650ac3439f9acc70af73c1bc0f0fa9976d114ffa7e7b5e9bc7c325779`。
- 候选 2,961 个文件逐项通过数据字节、描述符和 schema 核验；仓库内同步 11,509 字节 inline bootstrap。卡池目录 57 条，单页 80,060 字节；来源索引 61 条公告、336 条推定新卡关联及四类计数。57 条目录身份和顺序与当前生产索引一致。单条详情按需加载，保留名称来源和卡片关系证据。候选未发布。

## 前端与验收

- `gashas` 和 `gasha_detail` 直达、刷新、目录搜索、类型筛选及卡池来源返回使用 r6 read models，不触发旧 21 源 `/data` 批量读取。选择不同卡池、路由离开和失败重试均有异步回归。卡池关联卡跳到尚未迁移的卡片详情时才准备旧数据；卡片返回卡池保留来源路由。
- 相关启动、门户、导航状态、异步、展示及路由回归与 `npm run build:check` 通过。`build:check` 仅在 `.analysis/build-check` 编译代码，没有复制 `public` 语料；主 JS 约 606 kB（gzip 约 190 kB），仍有大 chunk 提示。
- Codex 应用内 Browser 使用 `127.0.0.1:5176` 的已构建代码、现有 `public` 和 r6 `pages`：直达目录显示 57/61/336，GROWING FES 筛选为 4/57，按关联卡名「幸福の象徴」搜索为 1/57；进入详情显示三张关联卡，详情刷新、卡池 → 卡片 → 返回、搜索结果 → 详情 → 返回均通过。该次旅程 Browser 无 console warning/error。服务日志显示纯卡池旅程仅请求 r6 卡池索引、目录页和详情；点击关联卡后才请求旧 `/data`。
- 此验证是本地生产代码与静态映射的 Browser 验收，不是完整媒体包、部署、真实设备或真实播放验收。其他路由仍待接线；`contracts/routes.json` 没有宣称完成迁移。
