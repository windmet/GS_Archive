# GS 组合资料 read-model 接线：2026-09-25

## 基线与范围

- 输入分支 `codex/gs-architecture-rebuild`，开始于 `d991779`。由已提交的组合投影生成 r5 候选 `E:\GS_readmodels_candidate_20260925_r5`，release `d1dd8d6c61322e3aafa2f58017f2f4638844ddd3bc2ca930a1e007a3b1bf4cc9`。2,961 个产物逐一通过数据字节、描述符和 schema 核验；仓库内同步 11,509 字节 inline bootstrap。候选未发布。
- 组合目录加载 16 条摘要，单页 59,819 字节；进入详情才加载一组记录，最大详情 28,835 字节。页面的成员、卡片统计、活动、歌曲和组合剧情来自 r5，未将完整成员卡片数组放进详情。16 个目录与详情产物逐项和当前 checkout 的生产投影比较，16/16 一致。
- 偶像档案与歌曲可直接进入组合详情；组合成员可直接进入偶像档案。卡片、活动与剧情等尚未迁移的目标在点击时准备旧数据。直达组合详情与返回来源页均保留路由身份。

## 验证

- 相关启动、导航、异步及展示回归、27 项 read-model 测试和 `npm run build:check` 通过。新增异步回归覆盖组合选择竞争、路由中断、失败重试和目录打开。`build:check` 只在 `.analysis/build-check` 编译代码，不复制 `public` 语料；主 JS 约 604 kB（gzip 约 189 kB），仍有大 chunk 提示。
- Codex 应用内 Browser 使用 `127.0.0.1:5176` 的已构建代码、现有 `public` 与 r5 `pages`：组合目录直达显示 16 组，Jupiter 详情显示 3 位成员、52 张卡片和关联资料；成员 → 偶像 → 返回、详情刷新、组合 → 卡片 → 返回、偶像 → 组合 → 返回均通过。r5 的 Home 与歌曲目录也完成简短回归。组合与偶像纯 read-model 旅程没有新控制台错误；Home 舞台产生两条 Spine 警告，画面仍显示。
- QA 服务日志显示纯组合旅程只请求 r5 组合索引、目录页和选中详情；进入卡片栏目后才请求旧批量 `/data`。本地代码与静态映射的 Browser 验收不等于完整媒体包、部署、真实设备或真实播放验收。
