# D：本机版本化阅读位置

起点 `fa5af4e`。每句提供“记住此处”，当前分段顶部可继续或清除记录。
每分段一个位置，最多保留 100 个分段；只由用户显式保存，不监听滚动自动认定已读。
不上传，不替代当前 URL，也不自动覆盖深链指定位置。

记录包含 document_id、manifest 文档 SHA、row_id、阅读语言和保存时间。
恢复必须匹配当前文档身份、版本及真实行；旧版本/缺失位置显示失效提示并禁用恢复，
允许清除或重新保存。恢复沿用 App 的 mode/locate 动作，播放器/阅读主状态不归
存储模块管理。清除只删除当前分段的记录，不触及其他分段和全站偏好。

存储 key 为 sidem.archive.reading-progress.v1。拒绝无效身份和跨文档行；损坏数据
不阻止阅读，存储拒绝/配额不足会给出保存失败反馈，不伪称成功。

验证：新增 verify-reading-progress 并纳入 verify:reading，覆盖版本失效、缺失行、
跨文档拒绝、损坏 JSON、同分段覆盖、100 条上限、隔离删除和存储异常。
既有阅读测试及 source-only 构建通过。Browser 在 390×844 验证第二章序章第二段
保存、刷新、显式继续到 step-3、清除当前记录；无横向溢出、无应用 error/warn。
浏览器仅通过页面操作验证，未直接读取或修改浏览器存储。

这不是自动阅读历史、跨设备同步或全站书签目录。跨篇检索及更多内容域仍待推进；
长稳继续后移，E2 未接管。
