# GS 卡片档案 read-model 接线：2026-09-25

## 基线与投影

- 输入分支 `codex/gs-architecture-rebuild`，开始于 `899e2ea`。卡片目录补充语音/剧情计数与共通系列身份，先以 `10f37a7` 单独提交并推送。由已提交投影生成只读候选 `E:\GS_readmodels_candidate_20260925_r7`，release `b7638c3257dcb1e59211b6b8735796f7f00d885e30790b992a2fa1b796851f57`。
- 候选 2,961 个文件通过字节、描述符和 schema 核验；仓库内同步 11,509 字节 inline bootstrap。826 张规范卡片分成 13 个目录页，共 1,022,089 字节原始 JSON；目录页均不超过 192 KiB。49 位偶像的卡片顺序与当前生产 `by_character` 去重后逐项一致；单卡完整资料按需读取。候选未发布。

## 前端与验收

- `cards` 和 `card_detail` 直达、刷新、稀有度/资源/关联筛选、偶像切换、前后卡及共通系列使用 r7 read models。卡片摘要的筛选结果在当前真实 826 张卡片上与旧选择器的 1,225 组条件逐项一致。卡池 → 卡片、卡片 → 卡池、卡片 → 偶像 → 返回均保留来源身份。剧情、活动等未迁移目标在点击时准备旧数据。
- 启动、导航状态、异步与卡片筛选回归及 `npm run build:check` 通过。`build:check` 只编译到 `.analysis/build-check`，未复制 `public` 语料；主 JS 约 609 kB（gzip 约 190 kB），仍有大 chunk 提示。
- Codex 应用内 Browser 使用 `127.0.0.1:5176` 的已构建代码、现有 `public` 与 r7 `pages`：直达冬馬卡片目录显示 19 张，SSR 显示 3 张；切换翔太显示 17 张，单卡详情刷新正确。卡池关联卡打开卡片、卡片关联卡池返回、卡片所属偶像返回，以及偶像 → 卡片 → 返回均通过。本地服务日志显示纯卡片与卡池 → 卡片旅程只请求 r7 目录/详情，没有旧 `/data`。卡片语音演出预览可进入 Player 并返回卡片；这次预览出现既有 Spine tint/update 警告与一条 LipSync 曲线 404，音频播放及画面稳定性未据此判定通过。
- 这是本地生产代码与静态映射的 Browser 验收，尚非完整媒体包、部署、真实设备或真实播放验收。其他路由仍待接线；`contracts/routes.json` 未宣称完成迁移。
