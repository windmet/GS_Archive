# Idol story read-model cutover, 2026-09-25

The attached architecture package remained a reference. This batch uses the checked-out selectors to publish a 49-idol switch directory with source-order identity, name, section and episode counts. Each idol story remains a separate detail leaf. The verified local r12 candidate is `E:\GS_readmodels_candidate_20260925_r12`, release `fa2f82602e1c8e0fedc2fcc64c7626c9f02c77d9134fa12120448f7649e84dbc`; all 2,961 artifacts verified. The inline bootstrap is 11,509 bytes. The first and last switch identities are `001tom` and `049eis`.

Direct `idol_story_archive` routes load the directory and selected detail without the legacy startup batch. Unknown idols go to the story picker. Opening and switching reject obsolete responses, and a failed switch can be retried without changing the selected idol. Reader and Player return to the selected story; a refreshed Player URL prepares its owner detail before restoring playback. The birthday archive link still loads the legacy story collection on demand. Returning from a direct idol story route prepares the legacy story catalog before showing it.

Verification: read-model tests, artifact verification, `npm run build:check`, startup/navigation regressions and the new idol-story async navigation regression passed. In-app Browser on the local QA server confirmed a direct idol story URL, switch between idols, unknown-idol picker and handoff, Reader return, refreshed Player return, birthday archive entry/return, and the return to a populated story catalog. This is local Browser evidence, not real-device or full-media acceptance. The story catalog, mobile archive and other remaining routes still need their own cutovers. No full asset package or deployment was created.
